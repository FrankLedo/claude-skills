#!/bin/bash
# Auto-approve Bash commands that operate within the slack-monitor plugin data directory.
# Without this, Claude Code prompts for permission on every file operation in the data
# directory because it is outside the project working directory.
set -euo pipefail

plugin_data="$HOME/.claude/plugins/data/slack-monitor"

input=$(cat)
cmd=$(echo "$input" | jq -r '.tool_input.command // ""')

if echo "$cmd" | grep -qF "$plugin_data"; then
  echo '{"hookSpecificOutput":{"hookEventName":"PreToolUse","permissionDecision":"allow","permissionDecisionReason":"Bash command targets slack-monitor plugin data directory"}}'
fi
